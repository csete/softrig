#pragma once


static void parse_response(sdriq_t *sdr, unsigned char *data, int length)
{
    (void) sdr;
    (void) data;
    (void) length;
    //fprintf(stderr, "data to parse: %d bytes\n", length);
}
