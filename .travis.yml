sudo: required

dist: trusty

language: cpp

compiler:
  - gcc

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq qt5-default libqt5svg5-dev libqt5multimedia5

script:
  - mkdir build
  - cd build
  - qmake ..
  - make

